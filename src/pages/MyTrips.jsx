// Placeholder — content intentionally left blank for now.
// Wired up just far enough (route + sidebar link, gated to Driver role)
// to test role-switching and page access.
export default function MyTrips() {
  return (
    <div className="flex flex-col items-center justify-center h-full text-center py-24">
      <h2 className="text-lg font-semibold text-arl-dark mb-2">My Trip</h2>
      <p className="text-sm text-arl-dark/50">
        Today's and upcoming trips will show here. Not built yet.
      </p>
    </div>
  );
}